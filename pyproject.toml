[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nummath"
version = "0.0.1"
authors = [
  {name = "jay"},
]
description = "A custom math library"
requires-python = ">=3.7"
license = {text = "MIT"}
keywords = ["math", "calculator", "equation"]
